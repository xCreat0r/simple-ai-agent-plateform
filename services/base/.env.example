# Base Service 配置模板（复制为 .env 使用；.env 不入库）
# 鉴权：HMAC 请求签名。多个 key:secret 对用逗号分隔（支持轮换：新 key 上线、旧 key 过渡期共存）。
# 生成 secret 方式: openssl rand -base64 32
# 该 key/secret 必须与后端 Worker 的 BASE_SERVICE_KEY / BASE_SERVICE_SECRET 对应其中一组
BASE_SERVICE_KEYS=key1:your-secret-here

# 应用层加密：与后端 Worker 的 PDF_ENCRYPTION_KEY 配置同一把（base64 编码的 32 字节密钥）。
# 配置后请求体按密文解密再解析；不配置则按明文（兼容/回滚）。
# 生成: openssl rand -base64 32
# PDF_ENCRYPTION_KEY=
